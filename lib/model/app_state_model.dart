import 'package:flutter/foundation.dart' as foundation;

// In the AppStateModel class, provide methods for accessing data from the model. 
// For example, add a method for accessing the shopping cart total, another for 
// a list of selected products to purchase, another for the shipping cost, and 
// so on.


class AppStateModel extends foundation.ChangeNotifier {
  // keep global objects + getters/setters here and update UI when changed.
}
